- name: 'Build'
  type: parallel
  steps:
      - service: us-locallayer-root
        command: exec -c 'echo warmed-up!'

- name: 'Test'
  type: serial
  service: nodejs-training-backend
  steps:
      - name: 'Run tests'
        command: run test
